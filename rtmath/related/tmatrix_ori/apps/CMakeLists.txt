# Build the apps
if (BUILD_CPP AND BUILD_APPS)

	#add_subdirectory(ice-server)
	#add_subdirectory(ice-client)
	#add_subdirectory(ellipsoid-proj)
	#add_subdirectory(batch-runs)
	#add_subdirectory(serial-runs)

	# Control option for creating qt apps
	#option ( MAKE_GUIS
	#	"Make Qt GUI programs?" OFF)

	#if (MAKE_GUIS)
	#add_subdirectory (indiv-run-qt)
	#endif()

endif()

